describe('authInterceptorService', function() {
    'use strict';
  beforeEach(module('<%= _.camelize(appname) %>'));

  it('should ...', inject(function(authInterceptorService) {

	//expect(authInterceptorService.doSomething()).toEqual('something');

  }));

});
